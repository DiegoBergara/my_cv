# Tasks: cta-navbar-improvements

## 1. CTAs más llamativos

- [x] 1.1 Mejorar variantes del componente Button en `src/components/ui/button.tsx` para mayor visibilidad (primario: más contraste y/o tamaño, posible sombra o hover marcado; secundario: manteniendo jerarquía pero claramente visible y clickeable)

## 2. Navbar transparente

- [x] 2.1 Cambiar el header en `src/components/Navbar.astro` a fondo transparente o muy sutil (quitar o reducir `bg-base/80` y `backdrop-blur-xl` según diseño) para que el contenido se vea detrás de la barra

## 3. Enlaces de la navbar resaltados

- [x] 3.1 Aplicar estilos en `Navbar.astro` para que los enlaces de navegación se vean claramente como clicables (por ejemplo mayor contraste, hover con fondo o borde sutil, o pill)
- [x] 3.2 Aplicar estilos en `Navbar.astro` para que el selector de idioma destaque como control clicable (por ejemplo borde o fondo que lo resalte)

## 4. Verificación

- [x] 4.1 Comprobar en hero y sección de contacto que los CTAs se ven más llamativos y en la navbar que es transparente y los ítems están resaltados (desktop y, si aplica, móvil)
